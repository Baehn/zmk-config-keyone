if SHIELD_KEYONE_LEFT

config ZMK_KEYBOARD_NAME
	default "keyone"

config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default y

endif

if SHIELD_KEYONE_LEFT || SHIELD_KEYONE_RIGHT

config ZMK_SPLIT
	default y

endif
